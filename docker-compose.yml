services:
  portfolio-frontend:
    container_name: portfolio_frontend
    build:
      context: . 
    restart: always
    networks:
      - vs_code_default
      
networks:
  vs_code_default:
    external: true
